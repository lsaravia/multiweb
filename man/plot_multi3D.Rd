% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_multiplex.R
\name{plot_multi3D}
\alias{plot_multi3D}
\title{3D Visualization of a Multiplex Ecological Network}
\usage{
plot_multi3D(
  g.list,
  layer.colors,
  as.undirected = T,
  layer.layout = "auto",
  layer.labels = "auto",
  layer.labels.cex = 2,
  edge.colors = "auto",
  edge.normalize = F,
  edge.size.scale = 1,
  node.colors = "auto",
  node.size.values = 0.5,
  node.size.scale = 1,
  node.alpha = 1,
  edge.alpha = 1,
  layer.alpha = "auto",
  layout = "fr",
  show.nodeLabels = F,
  show.aggregate = F,
  aggr.alpha = "auto",
  aggr.color = "#dadada",
  node.colors.aggr = "#dadada",
  layer.scale = 2,
  layer.shift.x = 0,
  layer.shift.y = 0,
  layer.space = 1.5,
  FOV = 30
)
}
\arguments{
\item{g.list}{A list of \code{igraph} objects, each representing a layer of the multiplex network.}

\item{layer.colors}{A vector of colors corresponding to each network layer.}

\item{as.undirected}{Logical; if \code{TRUE}, converts each layer to undirected for visualization purposes (default: \code{TRUE}).}

\item{layer.layout}{A layout matrix shared by all layers, or \code{"auto"} to use \code{layoutMultiplex()} (default: \code{"auto"}).}

\item{layer.labels}{A character vector for labeling layers. \code{"auto"} assigns "Layer 1", "Layer 2", etc. (default: \code{"auto"}).}

\item{layer.labels.cex}{Numeric; text size for layer labels (default: \code{2}).}

\item{edge.colors}{Edge colors (a single color or vector per layer), or \code{"auto"} to match layer color (default: \code{"auto"}).}

\item{edge.normalize}{Logical; if \code{TRUE}, normalizes edge weights using logarithmic scale (default: \code{FALSE}).}

\item{edge.size.scale}{Numeric or vector to control edge thickness (default: \code{1}).}

\item{node.colors}{\code{"auto"} to use layer colors, or an \verb{N x L} matrix for node colors per layer.}

\item{node.size.values}{\code{"auto"} to size nodes by strength, or a fixed numeric value (default: \code{0.5}).}

\item{node.size.scale}{Numeric or vector to scale node size (default: \code{1}).}

\item{node.alpha}{Transparency for nodes (0–1; default: \code{1}).}

\item{edge.alpha}{Transparency for edges (0–1; default: \code{1}).}

\item{layer.alpha}{A numeric vector with layer transparency, or \code{"auto"} to set 0.5 for all layers.}

\item{layout}{A character string indicating the layout method for \code{layoutMultiplex()} (default: \code{"fr"}).}

\item{show.nodeLabels}{Logical; if \code{TRUE}, node labels are shown (default: \code{FALSE}).}

\item{show.aggregate}{Logical; if \code{TRUE}, a layer with the aggregated network is shown (default: \code{FALSE}).}

\item{aggr.alpha}{Transparency for the aggregate layer (default: \code{"auto"} which is set to 0.5).}

\item{aggr.color}{Color of the background layer for the aggregate network (default: \code{"#dadada"}).}

\item{node.colors.aggr}{Node color used in the aggregate network (default: \code{"#dadada"}).}

\item{layer.scale}{Scale factor for the extent of each layer (default: \code{2}).}

\item{layer.shift.x}{Horizontal X shift per layer (default: \code{0}).}

\item{layer.shift.y}{Horizontal Y shift per layer (default: \code{0}).}

\item{layer.space}{Vertical spacing between layers along Z (default: \code{1.5}).}

\item{FOV}{Field of view for the \code{rgl} perspective visualization (default: \code{30}).}
}
\description{
This function generates a 3D visualization of a multiplex network composed of multiple layers
(e.g., trophic, mutualistic, and competitive networks) using \code{rgl}. Layers are displayed stacked
along the Z-axis. Node positions are shared across layers, and nodes can be colored by module
membership or functional type. An optional aggregate layer (merging all layers) can also be displayed.
}
\details{
Each layer is visualized as a semi-transparent plane, on which the respective network is plotted.
Nodes are shared across layers and maintain consistent positions. Optionally, the aggregated network
(union of all layers) can be visualized at the top. This function is designed for ecological multiplex
networks and facilitates interpretation of modular structure, interaction types, or perturbation effects.

This function is adapted from the \code{plot_multiplex3D()} function
in the \href{https://manlius.github.io/muxViz/}{\code{muxViz}} project.
}
\examples{
\dontrun{
library(multiweb)
fileName <- system.file("extdata", package = "multiweb")
dn <- list.files(fileName, pattern = "^Kefi2015.*\\\\.txt$")
g_list <- readNetwork(dn, fileName, skipColumn = 2)
names(g_list) <- c("Negative", "Positive", "Trophic")


# Visualize with community-colored nodes
plot_multi3D(
  g.list = g_list,
  layer.colors = RColorBrewer::brewer.pal(3, "Set2"),
  node.colors = "auto",
  node.size.scale = 0.5,
  node.size.values = "auto",
  node.colors.aggr = "#999999",
  show.aggregate = TRUE
)
}
}
