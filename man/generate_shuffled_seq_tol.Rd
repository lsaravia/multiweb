% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shuffling.R
\name{generate_shuffled_seq_tol}
\alias{generate_shuffled_seq_tol}
\title{Generate a Sequence of Shuffled Networks and Track SVD/Modularity Evolution}
\usage{
generate_shuffled_seq_tol(
  input_graph,
  delta = 10,
  max_iterations = 100,
  tolerance = 0.001,
  directed = TRUE,
  weighted = TRUE,
  modularity_func = cluster_infomap,
  shuffle_func = shuffle_network_deg
)
}
\arguments{
\item{input_graph}{An igraph object or adjacency matrix (directed or undirected, weighted or unweighted).}

\item{delta}{Integer. Number of link swaps per iteration (default: 10).}

\item{max_iterations}{Integer. Maximum number of iterations until network metrics stabilize (default: 100).}

\item{tolerance}{Numeric. Tolerance for metric stability - minimum change in modularity or SVD entropy (default: 1e-3).}

\item{directed}{Logical. Whether the network is directed (default: TRUE).}

\item{weighted}{Logical. Whether the network is weighted (default: TRUE).}

\item{modularity_func}{Function. Function to calculate modularity (default: \code{cluster_infomap}).}

\item{shuffle_func}{Function. Function to perform network shuffling (default: \code{shuffle_network_deg}).}
}
\value{
A list containing:
\describe{
\item{New_A}{Shuffled adjacency matrix}
\item{Metrics}{Data frame tracking metric evolution across iterations, including the original network}
}
}
\description{
This function generates a sequence of shuffled networks from an original graph by applying
incremental shuffling steps. It tracks modularity and SVD entropy throughout the process,
and stops when the change in these metrics falls below a specified \code{tolerance} threshold.
}
