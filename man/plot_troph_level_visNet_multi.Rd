% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotNetworkVis.R
\name{plot_troph_level_visNet_multi}
\alias{plot_troph_level_visNet_multi}
\title{Plot a Multiplex Ecological Network Organized by Trophic Level using visNetwork}
\usage{
plot_troph_level_visNet_multi(
  g,
  layer_attr = "layer",
  trophic_layer = "trophic",
  vertexSizeMin = 8,
  vertexSizeMax = 25,
  modules = FALSE,
  weights = NA,
  community_obj = NULL,
  node_weights = NA,
  module_spacing = 350,
  physics = c("x", "full"),
  highlight = TRUE,
  search = TRUE,
  label_size = 16,
  layer_colors = NULL,
  layer_dashed = NULL,
  layer_filter = TRUE
)
}
\arguments{
\item{g}{Either (a) a single \code{igraph} object with an edge attribute (named by
\code{layer_attr}) identifying the interaction type of each edge, or (b) a named list
of \code{igraph} objects, one per layer, all sharing (a subset of) the same vertex set
via \code{V(g)$name}. List names are used as layer labels.}

\item{layer_attr}{Name of the edge attribute holding the layer/interaction-type
label, used only when \code{g} is a single \code{igraph} object (default: \code{"layer"}).}

\item{trophic_layer}{Value identifying which layer represents trophic (feeding)
interactions; used to compute trophic level via \code{NetIndices::TrophInd()}
(default: \code{"trophic"}). Species with no trophic-layer edges (e.g. species that
only participate in non-trophic interactions) are assigned trophic level = 1
for display purposes, with a warning.}

\item{vertexSizeMin, vertexSizeMax}{Minimum/maximum node size (defaults: 8, 25).}

\item{modules}{Logical; if \code{TRUE}, nodes are grouped by community modules along
the x-axis, detected on the flattened (all-layers-pooled) network (default: \code{FALSE}).}

\item{weights}{Edge weights used for community detection when \code{modules = TRUE}
and \code{community_obj} is not supplied (default: \code{NA}).}

\item{community_obj}{Optional pre-computed community detection object.}

\item{node_weights}{Optional numeric vector used to size nodes. If \code{NA} (default),
total node degree pooled across all layers is used, so multiplex hubs are sized
according to their combined role across interaction types.}

\item{module_spacing}{Horizontal spacing multiplier between community modules
(default: 350).}

\item{physics}{Layout mode, \code{"x"} (default) or \code{"full"} — see
\code{\link[=plot_troph_level_visNet]{plot_troph_level_visNet()}} for details.}

\item{highlight}{Logical; hover-highlight nearest neighbors (default: \code{TRUE}).}

\item{search}{Logical; add node-search dropdown (default: \code{TRUE}).}

\item{label_size}{Node label font size in pixels (default: 16).}

\item{layer_colors}{Optional named character vector mapping layer names to edge
colors. If \code{NULL} (default), a \code{viridisLite} palette is generated automatically.}

\item{layer_dashed}{Optional character vector of layer names whose edges should be
drawn dashed, to distinguish them from solid edges regardless of color (typically
used to mark non-trophic layers, e.g. \code{layer_dashed = c("facilitation","competition")}).}

\item{layer_filter}{Logical; if \code{TRUE} (default), adds an interactive dropdown to
filter/isolate edges by layer.}
}
\value{
A \code{visNetwork} HTML widget.
}
\description{
Extends \code{\link[=plot_troph_level_visNet]{plot_troph_level_visNet()}} to multiplex ecological networks, where
species interact through several distinct types of interactions (e.g. trophic vs.
non-trophic: facilitation, competition, commensalism, amensalism) represented as
separate "layers" sharing the same set of nodes. Trophic level is computed using
only the trophic layer, while all layers are drawn together with distinguishable
edge colors/styles and an interactive layer filter.
}
