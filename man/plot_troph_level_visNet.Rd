% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotNetworkVis.R
\name{plot_troph_level_visNet}
\alias{plot_troph_level_visNet}
\title{Plot an Ecological Network Organized by Trophic Level using visNetwork}
\usage{
plot_troph_level_visNet(
  g,
  vertexSizeMin = 8,
  vertexSizeMax = 25,
  modules = FALSE,
  weights = NA,
  community_obj = NULL,
  node_weights = NA,
  module_spacing = 350,
  physics = c("x", "full"),
  highlight = TRUE,
  search = TRUE,
  label_size = 16
)
}
\arguments{
\item{g}{An \code{igraph} object representing the ecological network.}

\item{vertexSizeMin}{Minimum node size (default: 8).}

\item{vertexSizeMax}{Maximum node size (default: 25).}

\item{modules}{Logical; if \code{TRUE}, nodes are grouped by community modules along the x-axis (default: \code{FALSE}).}

\item{weights}{Edge weights used for community detection when \code{modules = TRUE} and \code{community_obj} is not
supplied (default: \code{NA}).}

\item{community_obj}{Optional pre-computed community detection object (e.g. from \code{cluster_spinglass()}).
If \code{NULL} and \code{modules = TRUE}, community detection is run internally via \code{cluster_spinglass()}.}

\item{node_weights}{Optional numeric vector used to size nodes. If \code{NA} (default), node degree is used.}

\item{module_spacing}{Horizontal spacing multiplier between community modules when \code{modules = TRUE}
(default: 350). When \code{modules = FALSE}, node x-coordinates are instead spread across a wide random
range scaled by \code{module_spacing * 1.5}.}

\item{physics}{Layout mode, one of \code{"x"} or \code{"full"} (default: \code{"x"}).
\itemize{
\item \code{"x"}: during the simulation, nodes can move horizontally but their
vertical position stays fixed at their trophic level, so the layout settles
into trophic bands.
\item \code{"full"}: nodes move freely in both x and y under a force-directed layout
(\code{forceAtlas2Based}), anchored by a small \code{centralGravity} to prevent
unbounded drift.
}
In both modes, once stabilization finishes, physics is disabled and every node's
\code{fixed.x}/\code{fixed.y} is unlocked, so the trophic-level ordering (in \code{"x"}) or the
force-directed layout (in \code{"full"}) is just the starting point — afterwards any
node can be freely dragged in any direction.}

\item{highlight}{Logical; if \code{TRUE}, hovering over a node highlights its nearest neighbors (default: \code{TRUE}).}

\item{search}{Logical; if \code{TRUE}, adds a node-search dropdown to the widget (default: \code{TRUE}).}

\item{label_size}{Font size, in pixels, used for node labels (default: 16). Apparent label size can
differ between \code{physics} modes even at the same value, since \code{"full"} typically stabilizes into a
more spread-out layout that is zoomed further out to fit the view; adjust this parameter if labels
look too large or too small for a given mode.}
}
\value{
A \code{visNetwork} HTML widget visualizing the trophic structure of the network.
}
\description{
This function visualizes a food web using \code{visNetwork}, where nodes represent species and edges represent interactions.
Nodes are positioned by trophic level on the y-axis, and optionally, community modules on the x-axis.
Unlike \code{\link[=plot_troph_level_ggplot]{plot_troph_level_ggplot()}} and \code{\link[=plot_troph_level_ggraph]{plot_troph_level_ggraph()}}, this produces an interactive HTML widget
that supports dragging, zooming, hovering for details, and node search.
}
